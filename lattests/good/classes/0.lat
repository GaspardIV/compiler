class Foo {
    int GetLength()
    {
        return length;
    }

    void SetLength(int value)
    {
        length = value;
    }

    int length;
}

int main() {
    Foo foo;
    // if (foo == null) {
        // foo = new Foo();
        // printInt(foo.GetLength());
    // }
    return 0;
}