class Foo {
    int GetLength()
    {
        return length;
    }

    void SetLength(int value)
    {
        length = value;
    }

    int length;
}

int main() {
    Foo foo;
    if (foo == (Foo)null) {
        foo = new Foo;
        int x = foo.length;
        printInt(x);
    }
    return 0;
}