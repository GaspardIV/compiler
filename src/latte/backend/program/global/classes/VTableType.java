package latte.backend.program.global.classes;

import latte.Absyn.Type;

public class VTableType extends Type {
    @Override
    public <R, A> R accept(Visitor<R, A> v, A arg) {
        return null;
    }
}
